<%- include('partials/header') -%>
  
<%- include('partials/footer') -%>

<script>
    const redirect = '<%= locals.redirect %>';
    setTimeout(() => {
        if (!redirect) {
            window.location = '/';
        }
        else {
            window.location = redirect;
        }
    }, 2000)
</script>